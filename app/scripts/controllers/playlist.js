//'use strict';
/*
var PlaylistCtrl = app.controller('PlaylistCtrl',
	['$rootScope', '$scope', '$location', 'PlaylistService', function ($rootScope, $scope, $location, PlaylistService) {
		$scope.tracks = [];

		$scope.nextTrack = function () {
			LastFmService.getTopAlbumsForArtist($scope.artist, function (data) {
				$scope.albums = data;
			});
		}


		$rootScope.$on('nextTrack', function () {
			console.log('next');
		});


		$scope.add = function(){

			alert('yep added');

		}

	}]);
*/
